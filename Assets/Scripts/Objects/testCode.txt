float speed = 1000;
int b = 0;

function rotate(sped) {
    rotation = rotation + sped * deltaTime();
    return rotation;
}

function main() {
    if (rotation < 360 && b < 2) {    
        print(rotate(speed));
    } else {
        b = b + 1;
        rotation = 0;
    }
}