float speed = 100;

function Setup()
{
    SetTarget(GetIntruderID());
}

function Main() {
    SetActive(true);
    Rotate(speed);
    if(rotation > 360){
        rotation = 0;
    }
}

function Rotate(sped) {
    rotation = rotation + sped * DeltaTime();
    return rotation;
}
