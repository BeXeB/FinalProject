float speed = 10;

function main() {
    rotate(speed);
    if(rotation > 360){
        rotation = 0;
    }
}

function rotate(sped) {
    rotation = rotation + sped * deltatime();
    return rotation;
}